import '..pulic/styles/index.css'

// This default export is required in a new `pages/_app.js` file.
export default function RoamingReviewApp({ Component, pageProps }) {
  return <Component {...pageProps} />
}